{
  "name": "swagger-to-client",
  "version": "1.0.0",
  "description": "swagger yaml parse to client entity object",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "p.wang",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^3.9.1"
  }
}
